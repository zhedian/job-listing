<div class="col-md-4 col-md-offset-4">
  <div class="group">
    <h2><%= @job.title %></h2>
    <p><%= @job.description %></p>
  </div>
    <%= link_to("Write a resume", new_job_resume_path(@job), class: "btn btn-default pull-right")%>
</div>
